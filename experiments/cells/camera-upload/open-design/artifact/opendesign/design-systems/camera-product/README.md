# Camera product design system

Sources: frozen PRD `input/source.md` and supplied screenshots `image-008.png` through `image-013.png`.

This task-scoped system captures the creation sheet, camera chrome, confirmation, review feedback, typography, color roles, 44px touch targets, and short ease-out transitions. It is intentionally not claimed as a canonical product-wide system.

