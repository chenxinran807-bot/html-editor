# Detected design patterns

- Evidence: frozen product mockups `image-009` through `image-013`.
- Mobile consumer shell with centered primary tabs and persistent bottom navigation.
- Secondary categories use rounded pills; active state is dark, inactive state is cool gray.
- Outfit feed uses two-column image cards, lime reason chips, compact purchase/try-on actions.
- Detail pages are single-column and explanation-led, with soft-gray content panels and product rows.
- Visual DNA is preserved; no desktop sidebar, breadcrumb, or invented admin shell.

