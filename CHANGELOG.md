# Changelog

## 1.3.3 — Working drag controls

- 修复蓝色边缘控制点在初始间距达到 160 px 时无法继续向外拖动的问题，范围扩展到 0–640 px。
- 在选中框左上角新增独立移动图标，可直接拖动选中元素并记录精确位移。
- 新增“160 px 后继续调整”与“拖动图标移动元素”两条回归测试。

## 1.3.2 — Empty-lane Inspector

- “添加修改”面板不再跟随点击坐标直接覆盖原型，优先停靠到页面左右空白栏。
- 空白不足时根据选中元素与页面主体的重叠面积选择遮挡最少的位置。
- 打开修改面板时暂时收起工具栏，关闭面板后自动恢复，减少重复占位。
- 支持拖动面板标题栏自由调整位置，并限制在当前视口范围内。
- 新增空白栏停靠与自由拖动两条交互回归测试。

## 1.3.1 — Non-blocking Inspector

- 标注工具默认固定在页面右侧空白区域，不再横跨页面底部。
- 注入后自动检测按钮、链接、输入框等核心交互区域，选择重叠最少的位置。
- 页面滚动或窗口尺寸变化时重新避让，降低遮挡保存、提交和返回按钮的风险。
- 新增交互区域避让契约测试，防止后续样式调整退回固定底栏。

## 1.3.0 — Precise Visual Controls

- 恢复页面常用色、系统原生颜色选择器和浏览器支持时的屏幕吸色。
- 面板按文字、容器和图片动态展示相关设置，不再堆叠笼统快捷词。
- 文字内容、字号、字重、行距、对齐、颜色、圆角、边框、阴影和图片裁切均可实时预览。
- 支持直接拖动选中元素四边调整“和旁边元素的距离”或“内部留白”，无需理解 margin、padding 或像素尺度。
- 每次控件操作输出确定的 `changes[]`，同时保留自然语言 `intent`；旧标注自动兼容为空数组。
- 预览取消或保存后均恢复业务页面原状，标注层不永久污染业务 DOM。
- 继续使用系统字体和内联 SVG，无外部 UI 框架、图标库、字体或网络依赖。

## 1.2.0 — Workflow Handoff

- 包装器新增 taskId、sessionId 和 prdFingerprint 三项可选绑定参数。
- 导出同时包含普通用户摘要与 `prd-demo-annotations` 结构化 JSON。
- 带 `data-prd-clause` 的节点优先输出稳定 `targetClauseId`；旧页面不伪造条款 ID。
- 所有修改请求明确为 `target-only`，供消费 Agent 做未受影响页面回归。
- 强制升级会同时移除旧 workflow meta，避免一个 HTML 绑定多个生成会话。
- 标注层继续只记录意图，不修改任何业务页面 DOM。

## 1.1.0 — macOS Overlay

- 将原有 emoji 密集工具条升级为 macOS 原生 Inspector 风格的 HTML 浮层。
- 用户主流程统一为“标记修改 → 我的修改 → 完成标注”。
- 修改面板使用“修改文字、更换图片、调整位置或大小、参考其他样式”等普通用户语言。
- 图标全部改为内联 SVG；字体使用系统字体；运行时不需要 UI 框架、图标库、字体文件或网络。
- 增加移动端底部面板、键盘退出、旧位置失效提示和复制失败兜底。
- CSS 选择器、HTML 片段和参考图数据继续保留在机器可读交付内容中，但不在用户界面展示。
- 保持原有点选、框选、参考图、localStorage、多页面 pin 和导出协议兼容。

### 升级旧页面

```bash
python3 scripts/wrap_annotator.py <old.html> -o <old.html> --force
```

`--force` 会移除旧标注脚本并注入 1.1.0，不会改写原页面业务结构。

### 后续方向

可选增加外部图标库、组件案例、字体排版预设和参考图库；这些能力只用于提供设计参考，不能成为标注工具正常运行的前置条件。
